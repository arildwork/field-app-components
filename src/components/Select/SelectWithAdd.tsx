import { FC } from "react";
import styles from "./Select.module.scss";

const FACSelectWithAdd: FC = () => {
  return (
    <div>
      <select></select>
    </div>
  );
};

export default FACSelectWithAdd;
